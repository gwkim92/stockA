import styles from "@/components/research/WorkspaceState.module.css";
export default function Loading() { return <section className={styles.skeleton} aria-busy="true" aria-label="리서치 자료 로딩"><p role="status" className={styles.loadingLabel}>리서치 자료를 불러오고 있습니다.</p><div aria-hidden="true" className={styles.skeletonTitle} /><div aria-hidden="true" className={styles.skeletonCards}>{[1,2,3,4].map(i => <div key={i} />)}</div><div aria-hidden="true" className={styles.skeletonContent} /></section>; }
