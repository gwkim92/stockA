# Review

## Summary

- Added `/api/ai/news-clusters` as a read-only frontend live endpoint.
- The endpoint surfaces stored `news_cluster_summary` AI artifacts with model invocation metadata, related cluster events, source documents, chunk count, and embedded chunk count.
- Added pagination support for the new collection endpoint.
- Added `AiNewsClusterListData`, `getAiNewsClusters()`, and a new `/intelligence` section that shows stored AI news analysis, RAG readiness, cost/no-live-LLM boundaries, and links to AI evidence, stock detail, and source documents.
- No vector URI, DB URL, secret value, recommendation scoring, or trading write path was added.

## Verification

- `PYTHONPATH=src /private/tmp/stockanalysis-runtime/venv/bin/python -m unittest tests.test_frontend_live_adapter.FrontendLiveAdapterTests.test_live_ai_news_cluster_list_response_matches_contract_shape tests.test_frontend_live_adapter.FrontendLiveAdapterTests.test_live_ai_news_cluster_list_sql_is_read_only -v`: 2 tests passed.
- `PYTHON_BIN=/private/tmp/stockanalysis-runtime/venv/bin/python bash scripts/verify_ai_news_cluster_map.sh`: passed.
- `cd apps/web && npm run typecheck`: passed after `.next/types` was regenerated by build.
- `cd apps/web && npm run build`: passed.
- Live FastAPI smoke: `/api/ai/news-clusters?asOfDate=2026-05-19&limit=4` returned cluster count 4, clustered event count 40, chunks 40, embedded chunks 40, local rule clusters 4, cost 0, no `vector_storage_uri`.
- Browser smoke: `/intelligence` shows stored AI analysis, AI news cluster count 4, embedding count 40, cost/no-live-LLM boundary, and AI evidence links.

## Residual Risks

- This is still local-rule AI evidence, not live LLM analysis.
- The cluster source text is RSS title/summary/URL only until a future free article body extraction task is added.
- The page lists stored clusters but does not yet provide interactive filters by theme, symbol, or source.
- This does not change recommendation quality, paper/live trading, benchmark, or evaluation split.
