# Topology

```text
parallel read-only specialists
  ├─ backend semantics
  ├─ product and approval risk
  ├─ tests and compatibility
  └─ historical terminology
             ↓
coordinator contract + implementation integration
             ↓
focused TDD → runner/CLI → additive data-health sibling
             ↓
independent backend review + independent risk review
             ↓
verification gate → handoff/review/QA → commit
```

The v2 artifact is a sibling shadow. No arrow connects it to the legacy authoritative readiness, outcome router, open-gate, professional-quality, scoring, portfolio, or order consumers in this task.
