# Notebook WebKit layout follow-up

Refined head 46fb1e22d740a0383190ce8507e5d53645bc7fe7 / run 34043394505 passed unit/build/type/audits and all 216 existing browser cases. Notebook results were 68/69 expected, with no skipped/flaky cases: all edit, save, reload, migration, export, storage failure/conflict and navigation cases passed on the three projects. The remaining failure was mobile WebKit page scrollWidth 480 at viewport 390. Main descendant bounding boxes did not protrude, so the earlier native date/fieldset size fix was not sufficient.

Archive 9992469132 was hash-verified as 1ae8ce5e05e16deaaee9c75f6d349655ae74fdf2a4fe37008b72b44c782108b9 and inspected. A temporary read-only full DOM/open-shadow-root geometry probe was added, without mutating UI or relaxing assertions. Probe head 60d3ef79ec7d340fe8fdd054410d81bf4f002761 / run 34044235672 isolates the overflow chain to sourcePicker: its bounding/client width is 320px but its scroll width is 445px, propagating to the source panel and document. Other main element bounding boxes remain within the viewport. The probe archive 9992660294 matched ff51a3fd38e87f55ec7caaab34306f430b529c40c0236ee2e9b23e0080fc7422. Notebook-first CI stopped before prior suites because the retained width assertion still failed; those skipped workflow steps are not counted as passed.

The source selector now has a separate explicit label, a minmax(0,1fr) grid track and non-native visual appearance with a non-interactive CSS chevron, while remaining a real select with full option labels and unchanged selection behavior. No html/body overflow clipping was added, no source text or control was hidden, and the 390px+1 threshold remains. The temporary probe file is removed; its full geometry collection is integrated into the existing layout test without adding counted cases. The whole notebook suite remains 69 cases and all five existing browser suites remain required.

Filled-note captures now use full-page screenshots from scroll origin with note bounds attached, instead of a tall element screenshot that can paint sticky/fixed navigation across the middle of the captured element. This changes capture positioning only, not UI style or visibility. Final cropped note images may be cut from these original full-page captures and must be identified as crops, not new rendered screens.

Final current-head CI and screenshots must be verified before integration. These follow-up observations are not a claim of physical iPhone testing, semantic investment quality or live data validation.
