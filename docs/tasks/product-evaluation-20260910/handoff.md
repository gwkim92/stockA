# 기능·화면 평가 인계

## 현재 결과

2026-09-10 평가 완료. 주 메뉴 20개를 데스크톱·모바일에서 확인하고 대표 상세 12개 경로, 주요 실패/개인 메모/모바일 탐색 흐름을 확인했다. 상세 3개는 모바일 추가 관찰. 관련 frontend 187개와 Python 25개 테스트 통과. 10개 항목을 High 3, Medium 6, Low 1로 정리했다.

최종 판단은 [report.md](report.md), 수행 범위는 [qa.md](qa.md), 코드 근거는 [implementation-review.md](implementation-review.md), 첫 화면 독립 판단은 [visual-review.md](visual-review.md)에 있다. 문서 작성과 증거 수집만 했고 제품 결함은 아직 수정하지 않았다.

## 다음 작업 순서

1. **ISSUE-002 / 종목 검색:** `/api/stocks`의 첫 50개를 넘어 서버 검색/페이지 이동 연결. NVDA 검색→상세 회귀 시나리오부터 재현한다.
2. **ISSUE-006 / 원천 링크:** 이번 CNM RSS ID의 Next 경로 파라미터·API 요청·HTTP 응답부터 추적한다. 목록 접두사 유무 두 경로 모두404이므로 접두사만 고치는 변경을 먼저 하지 않는다. 숫자형 ID뿐 아니라 콜론이 포함된 RSS ID로 검증한다.
3. **ISSUE-010 / 재무 입력:** NVDA 2012 지표와 2026 모델의 혼합을 재현하고 운영 forecast의 실제 원천 행 계보를 읽는다. 현재 조회/추정 SQL은 오래된 computed 값에 기간 제한이 없다. 기준 기간/허용 연령/입력 계보 및 사용 가능 gate를 함께 설계한다. 운영 추정/평가값 재생성은 별도 작업 범위를 정해 진행해야 한다.
4. **상태 의미:** AI 미조회 이력(003), 성과 실행 없음(005), 뉴스 지표 원천(004), 홈 반복 미종결 검토(001).
5. **화면 효율:** 근거 목록 중심 압축(008), 범위에 맞는 판단 문구·번역(009), 모바일 상태 표 가로 넘침(007).

추가 조사 대상인 데이터 상태 제목의 조건부 판정, 뉴스/데이터 상태 API 지연은 확정10개와 분리했다. 투자 성과·외부 공시 수치 검증과 이번 웹 기능 평가는 같은 작업이 아니다.

## 현재 환경과 경계

- 개인 EC2 `i-029d51b163fb07b61`, EIP `3.211.40.142`, 운영 웹 `f9f1d5ee0c5fca56b8ff8ddbe83e867ab1f87135`.
- 로컬 `develop@80769207c4a3610b8e8c21320c43ad2791a720a9`과 배포 commit 사이 차이는 AI 설정 task 문서 4개뿐임을 `git diff --stat`으로 확인했다. 앱 코드 검토 대상은 배포와 일치한다.
- 사용자 로컬 터널 `http://127.0.0.1:13309`과 Chrome 인증/설정은 유지한다. 운영 DB·모델·스케줄러·배포·투자 weight·실거래 변경은 하지 않았다.
- QA용 별도 브라우저에서만 임시 NVDA 노트를 생성했다. 수정·복원 확인 후 인라인 삭제 확인을 눌렀고 검토함이 빈 상태로 돌아온 것을 확인했다.
- `artifacts/product-evaluation-20260910/`에는 화면·DOM·재현 영상·캡처 스크립트·운영 readback을 저장했다. 이 경로는 로컬 증거이며 Git에 포함되지 않는다. 문서와 함께 검토할 때 로컬 파일을 유지한다.
- 이전 runtime-deploy/runtime-evidence-recovery task의 기존 수정/새 파일을 보존했다. 이번에는 제품 코드 변경, commit, push, 배포를 하지 않았다.

## 남은 검증

확정 결함이 수정되면 report의 후속 완료 기준으로 재검증한다. 전체 종목/문서/기준일·모든 필터·날짜 조합·부하·장시간 운영·전체 접근성·실물 모바일·실제 AI 호출·실거래는 이번 평가로 통과 처리하지 않는다. 사용자 Chrome 세션이나 운영 인증을 복사하지 말고 필요한 실행 범위 안에서 다시 확인한다.
