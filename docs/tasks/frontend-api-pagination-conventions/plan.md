# Implementation Plan

1. Add task contract, implementation plan, handoff, and review placeholders.
2. Add pagination helper with list endpoint specs, limit validation, opaque cursor encode/decode, and response metadata.
3. Wire pagination into fixture adapter, live adapter, FastAPI server, and stdlib fixture server.
4. Add unit tests for helper, fixture adapter, live adapter, and FastAPI error mapping.
5. Add top-level `pagination` metadata to collection examples and optional TypeScript response type.
6. Add pagination convention docs and verification script.
7. Update roadmap, verification plan, README, AGENTS, and task handoff/review.
8. Run verification and publish through PR.
