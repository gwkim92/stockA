# Task Plan

1. Create task contract, implementation plan, handoff, and review documents.
2. Add `get_data_operation_cadence()` to the cadence registry.
3. Add `src/stockanalysis/operations/artifact_runner.py`.
4. Add runner tests for success, failure, redaction, known job validation, and artifact root validation.
5. Add `stockanalysis-ingest data-operations-run` CLI and CLI test.
6. Add `scripts/verify_data_operations_artifact_runner.sh`.
7. Document the runner and update cadence docs, README, verification plan, roadmap, AGENTS, and dependent verification scripts.
8. Run artifact runner verification, cadence regression, roadmap verification, full unittest, AWH, and diff check.
