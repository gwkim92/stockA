# 자동 감시 경계

기존 scheduler-status 타이머가 단계 진행/기한·systemd 결과·설정 drift를 반복 읽고 운영 상태 파일을 갱신한다. 로그 출력이 없다는 이유만으로 작업을 실패 처리하지 않는다. 실행 중인 단계의 명시된 timeout과 전체 unit 시간 상한을 넘으면 중단 상태를 남긴다. 감시기는 임의의 DB 상태 변경·외부 알림 전송·완료 불명 작업 재실행을 하지 않는다. 자원 정책 rollback은 저장된 unit/container 설정을 복원하며 기존 타이머 상태를 유지한다.
