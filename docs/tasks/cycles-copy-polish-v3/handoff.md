# cycles-copy-polish-v3 handoff

## Status

- current status: in progress.
- completed: task contract created.

## Changes

- pending: polish `/cycles` copy and labels.

## Verification

- pending: `cd apps/web && npm run typecheck`
- pending: `cd apps/web && npm run build`
- pending: `PYTHONPATH=/Users/woody/ai/agent-work-harness/src python3 -m awh verify --repo . --task cycles-copy-polish-v3`
- pending: EC2 `/cycles` route/content smoke.

## Exact Next Step

- exact next step: edit `apps/web/src/app/cycles/page.tsx` copy and visible labels.

## Notes

- frontend visibility only.
- cycle scoring, recommendation, portfolio, broker/order, API, and DB logic are not changed.
