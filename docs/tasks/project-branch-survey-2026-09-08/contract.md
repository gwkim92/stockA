# Project and branch survey

- Request: understand `gwkim92/stockA` and investigate every currently published branch.
- Scope: clone into the initially empty workspace; inspect the `develop` integration baseline, all remote branch heads, commit ancestry, PR merge evidence, key implementation paths, schemas, CI, and verification boundaries.
- Deliverables: a Korean project report, complete branch inventory with immutable SHAs and integration classification, and a handoff with evidence and limitations.
- Changes: survey documents only. No application edits, branch merges, pushes, runtime deployment, external data collection, or trading actions.
- Completion: every real remote branch is accounted for; squash-merged history is distinguished from genuinely pending work; architecture claims are tied to current code; verification results are reported separately from documented historical runtime evidence.
